<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Lairbnb Lairs</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="styles.css">
  <style>
    /* Basic styling for layout */
    body {
      font-family: 'Gothic', Arial, sans-serif;
      margin: 0;
      padding: 0;
      background: #000; /* Black background */
      color: #fff; /* White text color */
    }

    .title-container {
      background-image: url('evil_lair_background.jpg'); /* Background image */
      background-size: cover;
      padding: 100px 20px; /* Padding top and bottom, adjust as needed */
      text-align: center;
      color: #ff0000; /* Red text color */
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 20px;
    }

    .card {
      margin-bottom: 20px;
      background: #222; /* Dark background for cards */
      border: 1px solid #333; /* Dark border */
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Shadow effect */
    }

    .card-img-top {
      height: 200px;
      object-fit: cover;
      position: relative;
    }

    .card-title {
      font-weight: bold;
      color: #ff0000; /* Red title color */
      font-size: 1.2em; /* Increase title font size */
    }

    .card-price {
      color: #ffcc00; /* Yellow price color */
    }

    /* Scary effect */
    h1, h2, h3, h4, h5, h6 {
      text-shadow: 2px 2px 4px #000; /* Text shadow effect */
    }

    .btn-primary {
      background-color: #ff0000; /* Red button background color */
      border-color: #ff0000; /* Red button border color */
    }

    .btn-primary:hover {
      background-color: #cc0000; /* Darker red on hover */
      border-color: #cc0000; /* Darker red border on hover */
    }

    .title-container::after {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.7); /* Semi-transparent overlay */
    }

    .title-container h1 {
      font-size: 3em;
      margin-bottom: 20px;
    }

    .title-container p {
      font-size: 1.2em;
    }

    /* Heart icon */
    .heart-icon {
      position: absolute;
      top: 10px;
      right: 10px;
      color: rgba(255, 0, 0, 0.8); /* Red heart color */
      font-size: 24px;
    }
  </style>
</head>
<body>

<!-- Title and Search bar -->
<div class="title-container">
  <h1 class="title my-4">All Lairbnb Lairs 😈</h1>
  <p>Find your perfect evil lair for a terrifying getaway!</p>
</div>

<!-- Search bar -->
<div class="search-bar">
  <div class="container">
    <form action="/search" method="GET" class="form-inline justify-content-center">
      <input type="text" name="query" class="form-control mr-sm-2" placeholder="Search" aria-label="Search">
      <button type="submit" class="btn btn-primary">Search</button>
    </form>
  </div>
</div>

<div class="container">
  <div class="row">
    <% @lairs.each do |lair| %>
      <div class="col-md-4">
        <div class="card">
          <div class="position-relative">
            <img src="local_image.jpg" class="card-img-top" alt="Lair Image">
            <span class="heart-icon">&#10084;</span> <!-- Heart icon -->
          </div>
          <div class="card-body">
            <h5 class="card-title"><%= lair.title %></h5> <!-- Only title -->
            <p class="card-text"><strong>Price:</strong> $<span class="card-price"><%= lair.price %></span> per night</p> <!-- Price -->
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

<!-- Opening Animation JavaScript -->
<script>
window.onload = function() {
    var audio = new Audio('evil_laughter.mp3');
    audio.play();
};
</script>

</body>
</html>
