<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Lairbnb Lairs</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="styles.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    /* Additional CSS styles */
    body {
      font-family: Arial, sans-serif; /* Use a clean and modern sans-serif font */
      background-color: white; /* Airbnb uses light backgrounds */
      color: #333; /* Dark text color */
    }

    .carousel-item img {
      height: 250px;
      border-radius: 10px; /* Add rounded corners to carousel images */
    }

    .lair-title {
      font-weight: bold;
      color: #333;
      font-size: 20px;
    }

    .lair-price {
      font-weight: bold;
      color: black; /* Use Airbnb's brand color for prices */
    }

    .btn-custom {
      background-color: black; /* Use Airbnb's brand color for buttons */
      color: white;
      border-color: black;
      border-radius: 17px; /* Add rounded corners to buttons */
    }

    .btn-custom:hover {
      background-color: gray; /* Darken the button color on hover */
      border-color: gray;
    }

    .btn-container {
      position: absolute;
      bottom: 20px;
      right: 20px;
    }

    .card {
      margin-bottom: 20px;
      box-shadow: none;
      border: none; /* Remove card border */
      border-radius: 20px; /* Add rounded corners to cards */
      background-color: white; /* White background for cards */
    }

    .card-body {
    background-color: white; /* Match the background color of the page */
    border: none; /* Remove card border */
    }

    .card-text .star {
    color: black;
    }

    .title-container {
    display: flex;
    align-items: center;
    }

    .card-title {
      flex: 1;
      font-size: 16px; /* Adjust the font size as needed */
    }

    .star {
      color: black; /* Set the color of the star symbol to white */
      font-size: 15px; /* Adjust the font size as needed */
    }
    
    /* Adjust map width */
    .map-container {
      width: 100%; /* Ensure the map container spans the full width of the page */
      height: 80vh; /* Adjust the height as needed (e.g., 80% of the viewport height) */
      position: relative; /* Ensure the map container is positioned relative to its parent */
    }

  </style>
</head>
<body>

<div data-controller="toggle">
  <div class="container" data-toggle-target="togglableElement">
    <h1 class="text-center my-4">All Lairbnb Lairs 😈</h1>


    <div class="row">
      <% @lairs.each do |lair| %>
        <div class="col-lg-3 col-md-4 col-sm-6 mb-4"> <!-- Adjust column widths for responsiveness -->
          <div class="card position-relative">
            <!-- Lair photo carousel -->
            <div id="carousel<%= lair.id %>" class="carousel slide" data-ride="carousel">
              <div class="carousel-inner">
                <% lair.photos.each_with_index do |photo, index| %>
                  <div class="carousel-item <%= 'active' if index.zero? %>">
                    <%= cl_image_tag photo.key, class: "d-block w-100 img-fluid" %>
                  </div>
                <% end %>
              </div>
              <a class="carousel-control-prev" href="#carousel<%= lair.id %>" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
              </a>
        <a class="carousel-control-prev" href="#carousel<%= lair.id %>" role="button" data-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carousel<%= lair.id %>" role="button" data-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="sr-only">Next</span>
        </a>
      </div>
      <div class="card-body">
      <div class="title-container">
        <h5 class="card-title lair-title"><%= lair.title %></h5>
        <p class="card-text"><strong></strong> <span class="star">&#9733;</span> <%= rand(2.0..5.0).round(2) %></p>
      </div>
        <p class="card-text"><strong>£</strong> <span class="lair-price"><%= lair.price %></span></p>
    <div class="btn-container">
        <%= link_to "View Lair", lair_path(lair), class: "btn btn-custom" %>
      </div>
    </div>
      <% end %>
    </div>
  </div>
  <button class="floating-btn" data-action="click->toggle#fire">Find your lair</button>
</div>
<h1 class="text-center my-4">Where the Lairs are 😈</h1>
<div class="mt-2">
  <div id="map-container" data-toggle-target="togglableElement2" style="height: 80vh;"
    data-controller="map"
    data-map-markers-value="<%= @markers.to_json %>"
    data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
</div>


<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
