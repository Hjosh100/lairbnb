<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Lairbnb Lairs</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="styles.css">
  <style>
    /* Additional CSS styles */
    body {
      font-family: Arial, sans-serif; /* Use a clean and modern sans-serif font */
      background-color: #f2f2f2; /* Airbnb uses light backgrounds */
      color: #333; /* Dark text color */
    }

    .carousel-item img {
      height: 250px;
      border-radius: 10px; /* Add rounded corners to carousel images */
    }

    .lair-title {
      font-weight: bold;
      color: #333;
      font-size: 20px;
    }

    .lair-price {
      font-weight: bold;
      color: #FF385C; /* Use Airbnb's brand color for prices */
    }

    .btn-custom {
      background-color: #FF385C; /* Use Airbnb's brand color for buttons */
      color: white;
      border-color: #FF385C;
      border-radius: 20px; /* Add rounded corners to buttons */
    }

    .btn-custom:hover {
      background-color: #FF0B5B; /* Darken the button color on hover */
      border-color: #FF0B5B;
    }

    .btn-container {
      position: absolute;
      bottom: 20px;
      right: 20px;
    }

    .card {
      margin-bottom: 20px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      border: none; /* Remove card border */
      border-radius: 20px; /* Add rounded corners to cards */
      background-color: #fff; /* White background for cards */
    }

    .card-body {
    background-color: #f2f2f2; /* Match the background color of the page */
    border: none; /* Remove card border */
    }


    /* Adjust map width */
    #map-container {
      width: 80%;
      margin: auto;
    }
  </style>
</head>
<body>

<div class="container">
  <h1 class="text-center my-4">All Lairbnb Lairs 😈</h1>

  <div class="row">
    <% @lairs.each do |lair| %>
      <div class="col-lg-3 col-md-4 col-sm-6 mb-4"> <!-- Adjust column widths for responsiveness -->
        <div class="card position-relative">
          <!-- Lair photo carousel -->
          <div id="carousel<%= lair.id %>" class="carousel slide" data-ride="carousel">
            <div class="carousel-inner">
              <% lair.photos.each_with_index do |photo, index| %>
                <div class="carousel-item <%= 'active' if index.zero? %>">
                  <%= cl_image_tag photo.key, class: "d-block w-100 img-fluid" %>
                </div>
              <% end %>
            </div>
            <a class="carousel-control-prev" href="#carousel<%= lair.id %>" role="button" data-slide="prev">
              <span class="carousel-control-prev-icon" aria-hidden="true"></span>
              <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#carousel<%= lair.id %>" role="button" data-slide="next">
              <span class="carousel-control-next-icon" aria-hidden="true"></span>
              <span class="sr-only">Next</span>
            </a>
          </div>
          <div class="card-body">
            <h5 class="card-title lair-title"><%= lair.title %></h5>
            <p class="card-text"><strong>Price:</strong> <span class="lair-price"><%= lair.price %></span></p>
            <div class="btn-container">
              <%= link_to "View Lair", lair_path(lair), class: "btn btn-custom" %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

<div id="map-container" style="height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
</div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
