 <%= simple_form_for([@lair, @booking]) do |f| %>
  <%= f.input :start_date, as: :string, class: "datepicker" %>
  <%= f.input :end_date, as: :string, class: "datepicker" %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
